#include <stdio.h>
/**
 * main - entry point
 * description - prints the lowercase alphabet
 * Return: 0
 */
int main(void)
{
int c;

for (c = 0; c < 26; ++c)
putchar('0' + c);

putchar('\n');

return (0);
}
